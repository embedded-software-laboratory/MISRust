\paragraph{Rule 19.2.2}
\begin{longtable}{p{13cm}}
\textbf{Analysis Result} \\
(C5) Rust-specific adaptation potentially required \\
\end{longtable}
\textbf{Comment:} As Rust does not use filenames, but rather crate names, and instead of the \texttt{include} directive the \texttt{use} directive, the guideline is not directly applicable. In general some restriction on valid import syntax in Rust is required (see reasoning guideline 19.2.3). \\
%no example exists
