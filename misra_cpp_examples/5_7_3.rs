fn main() {
    //Line splicing comment, that does not impact the next line: \
    println!("I am unaffected by the \\ in the line above");
    println!(
        "Multiline strings can use the escape character \\ to e.g. \
    do a linebreak without breaking the string"
    );
}
